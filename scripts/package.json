{
  "name": "workout-log-scripts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "seed": "tsx seed.ts",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
